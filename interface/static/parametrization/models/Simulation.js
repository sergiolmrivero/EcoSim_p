class Simulation {

	constructor(simulation_name, simulation_model, simulation_parameters, model_name) {

		this.simulation_name = simulation_name;
		this.simulation_model = simulation_model;
		this.simulation_parameters = simulation_parameters;
		this.model_name = model_name;

	}
}
