knitr::opts_chunk$set(echo = FALSE, message=FALSE, warning = FALSE, eval = TRUE)
### Generating the simulation Results
rm(list=ls())
### Generating the simulation Results
rm(list=ls())
```{r loadLibs}
### Getting the Libraries
library(tidyverse)
```{r setupDirs}
### Setting up the Directories
i  <-  "runs"
o  <-  "results"
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "\\.csv$")
csv_files
runs_df  <- csv_files %>%
map_dfr(read_csv)
View(runs_df)
names(runs_df)
ggplot(runs_df, aes(y = consumtpion, color = as.factor(employed))) +
geom_boxplot()
View(runs_df)
ggplot(runs_df, aes(y = consumption, color = as.factor(employed))) +
geom_boxplot()
ggplot(runs_df, aes(y = total_cash, color = as.factor(employed))) +
geom_boxplot()
ggplot(runs_df, aes(x = step, y = consumption)) +
geom_line()
ggplot(means_df, aes(x = step, y = total_cash)) +
geom_line() +
facet_grid(as.factor(run)~.)
ggplot(runs_df, aes(x = step, y = total_cash)) +
geom_line() +
facet_grid(as.factor(run)~.)
ggplot(runs_df, aes(x = consumption, y = total_cash)) +
geom_point()
ggplot(runs_df, aes(y = total_cash)) +
geom_histogram()
ggplot(runs_df, aes(x = total_cash)) +
geom_histogram()
ggplot(runs_df, aes(x = consumption)) +
geom_histogram()
knitr::opts_chunk$set(echo = FALSE, message=FALSE, warning = FALSE, eval = TRUE)
### Generating the simulation Results
rm(list=ls())
### Setting up the Directories
i  <-  "runs"
o  <-  "results"
### Getting the Libraries
library(tidyverse)
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "\\.csv$")
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
runs_df  <- runs_df[complete.cases(runs_df),]
runs_df$diff_payoffs  <- runs_df$my_payoff - runs_df$other_payoff
ggplot(runs_df, aes(y = consumption, color = as.factor(employed))) +
geom_boxplot()
ggplot(runs_df, aes(y = consumption, color = as.factor(employed))) +
geom_boxplot()
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
runs_df  <- runs_df[complete.cases(runs_df),]
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
View(runs_df)
View(runs_df_or)
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "macro_Caiani_HH\\.csv$")
```{r generatingDF}
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
runs_df  <- runs_df[complete.cases(runs_df),]
runs_df$diff_payoffs  <- runs_df$my_payoff - runs_df$other_payoff
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "macro_Caiani_HH\\.csv$")
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "macro_Caiani_HH \\.csv$")
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "^macro_Caiani_HH.*\\.csv$")
```{r generatingDF}
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
runs_df  <- runs_df[complete.cases(runs_df),]
runs_df$diff_payoffs  <- runs_df$my_payoff - runs_df$other_payoff
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "^macro_Caiani_HH*\\.csv$")
```{r generatingDF}
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
runs_df  <- runs_df[complete.cases(runs_df),]
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "^macro_Caiani_HH*")
```{r generatingDF}
runs_df  <- csv_files %>%
map_dfr(read_csv)
### Getting the files
csv_files <- fs::dir_ls(i, regexp = "macro_Caiani_HH*")
```{r generatingDF}
runs_df  <- csv_files %>%
map_dfr(read_csv)
runs_df_or  <-  runs_df
runs_df  <- runs_df[complete.cases(runs_df),]
runs_df$diff_payoffs  <- runs_df$my_payoff - runs_df$other_payoff
View(runs_df)
runs_df  <- runs_df[complete.cases(runs_df),]
```{r grafsFromRunDF}
ggplot(runs_df, aes(y = consumption, color = as.factor(employed))) +
geom_boxplot()
ggplot(runs_df, aes(y = total_cash, color = as.factor(employed))) +
geom_boxplot()
ggplot(runs_df, aes(y = consumption, color = as.factor(employed))) +
geom_boxplot()
ggplot(runs_df, aes(x = step, y = consumption)) +
geom_line()
ggplot(runs_df, aes(x = step, y = consumption)) +
geom_line()
ggplot(runs_df, aes(x = step, y = total_cash)) +
geom_line() +
facet_grid(as.factor(run)~.)
ggplot(runs_df, aes(x = consumption, y = total_cash)) +
geom_point()
